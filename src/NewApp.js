import React from 'react';
import Routing from './Routing'; // Importing the new routing component
import './index.css';

const NewApp = () => {
  return (
    <Routing /> // Using the Routing component
  );
};

export default NewApp;
